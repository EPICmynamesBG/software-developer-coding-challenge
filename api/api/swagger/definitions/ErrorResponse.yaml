required:
  - statusCode
  - message
properties:
  statusCode:
    type: number
  message:
    type: string
  action:
    type: string
    description: some modifying action type
    enum:
      - CREATE
      - UPDATE
      - DELETE
